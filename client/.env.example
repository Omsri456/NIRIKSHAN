# In development, requests to /api are proxied to http://localhost:5000 by Vite (see vite.config.ts).
# Leave this unset for local development.
#
# For a production build served separately from the API, set this to the API's origin:
# VITE_API_BASE_URL=https://api.example.gov.in
